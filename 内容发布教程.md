# 网站内容发布教程

这份教程用于给这个个人网站新增文章、上传图片或附件，并发布到线上。日常只需要改内容文件，不需要改网站程序。

网站地址：<https://xuyuxiu83.github.io>

## 最常用的流程

1. 在 `_posts` 文件夹新建一篇 Markdown 文章。
2. （如有）把图片放进 `images/`，PDF 等附件放进 `files/`。
3. 在电脑上预览或检查文件名、链接和文章内容。
4. 将改动提交并推送到 GitHub 的 `main` 分支。
5. 等待 GitHub Pages 自动构建完成，打开网站确认。

## 一、新建一篇文章

所有正式文章都放在 `_posts/`。文件名必须遵循下面的格式：

```text
YYYY-MM-DD-英文短标题.md
```

例如，2026 年 9 月 18 日写一篇读书笔记：

```text
_posts/2026-09-18-thinking-in-systems.md
```

> 文件名中的日期决定文章归档的年份和月份；短标题建议只用小写英文、数字和连字符 `-`，不要使用空格或中文。

将下面的内容复制到新文件中，再按需修改：

```markdown
---
title: "文章标题写在这里"
date: 2026-09-18
categories: [personal-thoughts]
tags: [标签一, 标签二]
---

这里开始写正文。

## 小标题

普通段落直接输入文字即可。**加粗文字**用两侧各两个星号包起来，
*斜体文字*用一对星号包起来。

- 无序列表第一项
- 无序列表第二项

1. 有序列表第一项
2. 有序列表第二项

[这是一个链接](https://example.com)
```

写完后，文章会出现在「归档」页面，并按日期排序；不需要手动把它加到导航栏或首页。

### 选择分类

`categories` 目前支持以下四种值。每篇文章建议只选一种：

| 想写的内容 | 填写的分类 |
| --- | --- |
| 个人随想、生活记录 | `personal-thoughts` |
| 论文阅读、研究笔记 | `paper-notes` |
| 读书笔记 | `book-notes` |
| 项目过程、技术复盘 | `project-notes` |

例如，将 `categories: [personal-thoughts]` 改成 `categories: [book-notes]`，这篇文章就会归入「读书笔记」。

### 暂时不发布的草稿

还没写完的文章请放在 `_drafts/`，例如 `_drafts/my-idea.md`。草稿不会出现在正式网站上。写完后，把文件移动到 `_posts/`，并按前面的规则改成带日期的文件名，再发布。

仓库内的 `_drafts/post-draft.md` 是可直接复制使用的写作模板。

## 二、插入图片和上传附件

### 图片

把图片放入 `images/`；为了便于管理，可以按文章建子文件夹，例如：

```text
images/thinking-in-systems/cover.png
```

然后在文章中这样引用：

```markdown
![图片说明](/images/thinking-in-systems/cover.png)
```

`图片说明` 会在图片无法加载时显示，也有利于读者理解图片内容。图片文件名建议使用英文、数字与连字符，不要用空格。

### PDF、幻灯片和其他可下载文件

把 PDF、ZIP、PPTX 等文件放入 `files/`，例如：

```text
files/my-paper.pdf
```

在文章中创建下载链接：

```markdown
[下载论文 PDF](/files/my-paper.pdf)
```

不要把很大的视频直接放进仓库；这会让网站仓库变得很重。视频建议上传到 Bilibili、YouTube 或网盘后，在文章中放链接。

## 三、写作时的几个常用语法

````markdown
# 一级标题（正文中通常不需要，因为文章标题已经在 title 里）
## 二级标题
### 三级标题

> 这是一段引用。

`行内代码`

```python
# 代码块：三个反引号后可标注语言
print("Hello")
```

---

上面是一条分隔线。
````

Markdown 支持大多数常用写法。若只是写文章，记住「空一行分段、`##` 写小标题、`-` 写列表、`[文字](链接)` 写链接」就足够了。

## 四、本地预览（可选，但建议）

如果已配置好 Docker，在仓库根目录打开终端并运行：

```bash
docker compose up
```

然后在浏览器打开 <http://localhost:4000>。文章或图片修改后，页面通常会自动刷新；停止预览时，在终端按 `Ctrl+C`。

第一次使用或修改了网站依赖时，先运行：

```bash
docker compose build --progress=plain
docker compose up
```

如果本地 Docker 因网络问题无法构建，也可以直接上传到 GitHub，再查看 GitHub Actions 的构建结果。

## 五、上传并发布到 GitHub

在仓库根目录打开终端，依次执行：

```bash
# 查看这次改了哪些文件
git status

# 将这次实际修改的文件加入提交（把路径换成自己的文件名）
git add _posts/2026-09-18-thinking-in-systems.md

# 若新增了图片或附件，也把它们加上，例如：
git add images/thinking-in-systems/cover.png files/my-paper.pdf
git commit -m "Add post: thinking in systems"

# 上传到 GitHub，并触发网站发布
git push origin main
```

提交说明（`-m` 后的英文）只用于记录这次做了什么，可以自由改写；例如 `Add book note`、`Update profile`。

上传后，打开 GitHub 仓库的 **Actions** 标签页，等待「Jekyll build」成功（绿色勾）。通常几分钟内网站会更新；随后打开网站或「归档」页面确认文章是否显示。

### 使用 GitHub 网页上传（不使用终端）

也可以在 GitHub 仓库网页完成简单更新：

1. 打开仓库，进入 `_posts` 文件夹，点击 **Add file → Create new file**。
2. 输入文件名，例如 `2026-09-18-my-new-post.md`，粘贴文章内容。
3. 在页面底部填写提交说明，点击 **Commit changes**。
4. 图片和附件则进入 `images` 或 `files` 文件夹，点击 **Add file → Upload files** 上传并提交。
5. 到 **Actions** 查看构建成功后，再打开网站检查。

网页方式适合小改动；写长文或一次上传多张图片时，建议使用本地编辑器加 Git 命令。

## 六、发布前检查清单

- 文件在 `_posts/`，文件名是 `日期-英文短标题.md`。
- 文件最开头有完整的 `---`、`title`、`date`、`categories` 和结尾的 `---`。
- `date` 与文件名日期一致，格式为 `YYYY-MM-DD`。
- 分类是上表中的一个值。
- 图片路径以 `/images/` 开头，附件路径以 `/files/` 开头。
- 文中链接可以正常打开，图片能显示。
- `git push origin main` 已成功，GitHub Actions 显示绿色勾。

## 七、出问题时怎么排查

**文章没有出现**：先检查文件是否在 `_posts/`，文件名是否以有效日期开头，以及 front matter（开头的 `---` 区块）是否完整。

**图片不显示**：检查图片是否已上传、大小写是否完全一致，以及路径是否写成 `/images/文件名.png`。

**网站没有更新**：确认 `git push origin main` 没有报错；再在 GitHub 的 Actions 中打开失败的「Jekyll build」，按错误提示检查对应 Markdown 文件。最常见的原因是 YAML 头部少了引号、冒号或 `---`。

**误发布了文章**：把文章从 `_posts/` 移回 `_drafts/`，或删除该文件，然后重新提交并推送。Git 有提交历史，通常可以找回此前版本。
